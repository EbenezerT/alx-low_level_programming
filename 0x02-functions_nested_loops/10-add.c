#include "main.h"
/**
 * add - function that adds two numbers
 * @a: first parameter
 * @b: Second parameter
 * Return: Always return 0
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
