#include "main.h"

/**
 * main - A function that prints it name
 * @argc: gets the number of elements
 * @argv: An array of pointers to the arguments.
 * Return: returns 0 on success
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
